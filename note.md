###
1. Virtual DOM is a copy of real DOM
2. we have two virtual DOMs
    first virtual DOM is created at the time of mounting of react component
    second manages the changes

###
React Features
1. Virtual DOM
2. Single page Application
3. JSX and Babel
4. One Way Binding
5. Component Based
###
HOOKS
1. HOOKS are functions 
2. there is 2 rules to use HOOKS
    a> only calls HOOKS at the Top level
    b> only calls HOOKS from React function
3. HOOKS are only define in react function  